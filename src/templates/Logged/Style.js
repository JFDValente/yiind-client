import styled from 'styled-components';

const Content = styled.div`
  display: flex;
  flex-direction: row;
  width: calc(100% + 32px);
  background-color: #23B5B5;
`;

export default { 
  Content,
}